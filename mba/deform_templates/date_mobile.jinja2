  <input type="date"
         name="{{ field.name }}"
         value="{{ cstruct }}"
         {% if field.widget.size %}
         size="{{ field.widget.size }}"
         {% endif %}

         class="form-control {% if field.widget.css_class %} {{ field.widget.css_class }} {% endif %}"

         id="{{ field.oid }}"/>

